routes:
  # ─── Health check ────────────────────────────────────────────────────
  - id: health
    uri: /__health
    methods: [GET]
    priority: 100
    plugins:
      serverless-pre-function:
        phase: access
        functions:
          - |
            return function(conf, ctx)
              ngx.header["Content-Type"] = "application/json"
              ngx.say('{"status":"ok"}')
              return ngx.exit(200)
            end

  # ─── FHIR Metadata — no auth ──────────────────────────────────────────
  - id: int-metadata
    uri: /fhir/metadata
    methods: [GET]
    priority: 20
    upstream_id: hapi-${PARTY}
    plugins:
      proxy-rewrite:
        uri: /fhir/${PARTY}/metadata

  # ─── Own FHIR data — JWT + realm-role + URL rewrite ──────────────────
  # Serves only this party's own partition. The web-app authenticates cross-party
  # calls itself (in-browser client_credentials) and talks directly to the
  # partner's external gateway.
  - id: int-fhir
    uri: /fhir/*
    methods: [GET, POST, PUT, PATCH, DELETE]
    priority: 1
    upstream_id: hapi-${PARTY}
    plugins:
      openid-connect:
        discovery: "${KEYCLOAK_DISCOVERY_URL}"
        bearer_only: true
        use_jwks: true
        set_access_token_header: true
        set_userinfo_header: false
        client_id: "unused"
        client_secret: "unused"
      umzh-role-check:
        required_role: "${PARTY}"
      proxy-rewrite:
        regex_uri: ["^/fhir/(.*)$", "/fhir/${PARTY}/$1"]

  # ─── OPA policy check passthrough (local diagnostic) ─────────────────
  - id: policy-check
    uri: /api/policy/check
    methods: [POST]
    upstream_id: opa-${PARTY}
    plugins:
      proxy-rewrite:
        uri: /v1/data/umzh/authz/allow

upstreams:
  - id: hapi-${PARTY}
    nodes:
      "nginx-proxy:${NGINX_OWN_PORT}": 1
    type: roundrobin

  - id: opa-${PARTY}
    nodes:
      "opa-${PARTY}:8181": 1
    type: roundrobin

global_rules:
  - id: 1
    plugins:
      cors:
        allow_origins: "**"
        allow_methods: "GET,POST,PUT,PATCH,DELETE,OPTIONS"
        allow_headers: "Authorization,Content-Type,Accept,X-Tenant"
        expose_headers: "Content-Length,Content-Type"
        max_age: 43200
        allow_credential: true

#END
